<%- include('partials/header') %>

<div class="container mt-5">
  <h1>Borrow</h1>

  <div class="row">
    <div class="col-sm-8">
      <div class="card">
        <div class="card-body">

          <!-- Makes POST request to /login route -->
          
          <form action="/student/borrow" method="POST">
            <div class="form-group">
              <label for="book">Name of Book</label>
              <input type="text" class="form-control" name="bookname">
            </div>
            <br>
            <br>
            <button type="submit" class="btn btn-dark" name="user" value="<%= username %>">Search</button>
            <br>
            
          </form>
          <% if(count === 1){%>
          <p>Book borrowed already🤐</p>
          <% } %>
          <% if(count === 2){%>
          <p>Book Not Found🤐</p>
          <% } %> 
        </div>
      </div>
    </div>

    <!-- <div class="col-sm-4">
      <div class="card">
        <div class="card-body">
          <a class="btn btn-block btn-social btn-google" href="/auth/google" role="button">
            <i class="fab fa-google"></i>
            Sign In with Google
          </a>
        </div>
      </div>
    </div> -->

  </div>
</div>

<%- include('partials/footer') %>
